# Potassium Clicker Test

A small Vercel-ready mock storefront for testing browser DOM click watchers.

## Local testing

Open `index.html` directly in a browser, or run:

```bash
npm install
npm run dev
